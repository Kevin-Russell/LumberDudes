package bb.common;

/**
 * ActionType is an enumeration that encompasses
 * all possible actions that the client can send to the server.
 */
public enum ActionType {
	LEFT,
	RIGHT,
	JUMP,
	NONE
	// More? ...
}
